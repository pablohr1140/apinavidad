NODE_ENV=development
PORT=3000
LOG_LEVEL=info
PASETO_SECRET=replace-with-64-byte-secret----------------------------------------------------------------
WEB_APP_ORIGIN=http://localhost:5173
COOKIE_DOMAIN=localhost

# Ajusta credenciales según tu instancia
DB_HOST=localhost
DB_PORT=1433
DB_USER=sa
DB_PASSWORD=YourStrong!Passw0rd
DB_NAME=infancias
DB_SCHEMA=dbo
DB_SYNCHRONIZE=false
# Prisma usa esta URL; si la omites se arma automáticamente con los valores anteriores
DATABASE_URL="sqlserver://localhost:1433;database=infancias;user=sa;password=YourStrong!Passw0rd;encrypt=false;trustServerCertificate=true"

# Feature flags ORM dual-stack
PRISMA_READS=false
PRISMA_WRITES=false

# Cache distribuida (Redis)
REDIS_URL=redis://localhost:6379
# Alternativamente define host/puerto si no usas URL completa
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Credenciales iniciales del admin seed
DEFAULT_ADMIN_EMAIL=admin@example.com
DEFAULT_ADMIN_PASSWORD=ChangeMe!123
